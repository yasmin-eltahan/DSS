<%= form_for(@type , :as=>:type, :url=>{:action=>'create'}) do |f| %>
<br>
<br>
<dd><%= f.text_field(:name)%></dd>
<%= f.fields_for :requirements do |builder| %>
  <fieldset>
  <%= builder.label :name, "requirements" %><br/>
  <%= builder.text_field :name %>
  </fieldset>
<% end %>
 <% end %>