<% @types.each do |type| %>
	<div class="box-list">
		<div style="padding-left:20px;">

		<div class="row">
		<div class="span4" style="margin-top:5px;">
		<h4><b class="link-color" ><%= type.name %></b></h4>
		</div>
		<div class="span3 offset4 pull-right" style="padding-top:9px;padding-left:73px">
		<a href="#<%= type.id %>" role="button"  data-toggle="modal"  class="btns btn-mini"> <i class="icon-pencil icon-white" title="edit" ></i></a>
		<span style="padding-left:8px"><%= link_to({:action => 'destroy', :id=>type.id},:confirm => 'Are you sure you want to delete?!' , :class=>"btns btn-mini") do  %> <i class="icon-trash icon-white" title="delete"></i> <%end%> </span><br>
		</div>
		</div>

		<div>
	    <div id="<%= type.id %>" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
	      <div class="modal-header">
	        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
	        <h3 id="myModalLabel">Edit Requirement</h3>
	      </div>
	      <div  class="score-div padding">
	        <% @edit = Type.find(type.id) %>
	        <%= form_for(@edit , :as=>:type, :validate => true,:url=>{:controller => 'types' , :action=>'update', :id=>type.id}) do |f| %>
			<%= render(:partial=>'main', :locals=>{:f=>f}) %>
			<%end%>
	      </div>
	    </div>
	    </div>

	    <div class="line-break">
	    <br>
	    Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do eiusmod
	    tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam,
	    quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo
	    <br>
	    </div>
	    Category: <% if (type.technical.to_s == "true") %> Technical <%else%> Business <%end%><br><br>

		<div style="padding-left:5px;">
		<span class="link-color-purple"><b>&#9734; <%= type.name%> requirements</b></span><br><br>
			<ul>
			<% type.requirements.each do |req|%>
			<li><%= req.name %></li>
			<%end%>
			</ul>
		</div>
		<br>
	</div>
	</div>
	<br>
<%end%>